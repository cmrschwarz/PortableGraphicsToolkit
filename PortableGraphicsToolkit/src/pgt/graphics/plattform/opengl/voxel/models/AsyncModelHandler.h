#pragma once
namespace pgt {
    class AsyncModelHandler {
      public:
        // TODO use pimpl to implement async functionality of ModelManager in
        // here to save space for users not needing asynchronuous capabilitys
        // (evaluate, cause these users don't exist) not really high priority,
        // cuase this is an implementation detail that can be changegd without
        // much effort at any time
    };
}